# The git URL from which is to be cloned
REPOURL=git://github.com/foo/bar.git

# The directory used for cloning into and building
BUILD=/home/foo/build/bar

# The branch to build
BRANCH=master

# The output files generated by the build that are to be uploaded. They
# aren't checked for existance or anything, just passed litterally to
# the upload script (see below)
OUTFILES="/home/foo/build/bar/bar-latest.xpi[application/x-xpinstall] /home/foo/build/bar/bar-latest-update.rdf[application/rdf+xml]"

# A shell command line which the build bot will run after a successful
# build for each file in $OUTFILES. This will be passed literally to
# sh -c, the current outfile is in $0 - do what thou whilst.
UPLOAD='./github-upload "$0" foo/bar'

# The mail address to send the status output to
MAIL_TO=foo@bar.baz

# The subject for the status mail
MAIL_SUBJ="buildbot output for foo repository"
